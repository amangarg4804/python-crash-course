names = ["Carlos", "Ray", "Alex", "Kelly"]
# names.sort() will sort the original list
print(sorted(names)) # sorted function will return the new sorted list
print(names)
print(sorted(names, key=len)) # key allows the function that can be used for sorting